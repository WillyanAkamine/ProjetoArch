docker-compose up -d --build // Atulizar container com mudanças.

composer install
composer update


git status // Ver status do commit

git commit -m 

git add e o nome do arquivo alterado

git log --oneline // Ver commits 

git push origin main // Subir projeto para origin(Nuvem)

git pull origin main // Atulizar brach 

git checkout main // trocar branch.

git checkout -b nome_branch // Criar uma branch nova e ja desloca ela.

./vendor/bin/doctrine-migrations

